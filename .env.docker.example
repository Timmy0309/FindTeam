# Скопируйте при необходимости в backend/.env для локального запуска без Docker
PORT=5000
JWT_SECRET=change_me_in_production

DB_HOST=localhost
DB_PORT=5432
DB_NAME=gameteam_db
DB_USER=postgres
DB_PASSWORD=postgres
